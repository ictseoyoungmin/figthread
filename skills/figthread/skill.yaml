schema: hadara.skill/v0.1
name: figthread
version: 0.3.0
description: >-
  Design and validate semantic figures through promoted semantic state,
  deterministic promoted layout geometry, and deterministic semantic motion.
entry:
  skill: SKILL.md
resources:
  templates: templates/
  references: references/
  assets: assets/
  schemas: schemas/
  runtime: runtime/
  scripts: scripts/
  examples: examples/
compatibility:
  agents: [Claude Code, Codex CLI]
  os: [windows, linux, macos]
interface:
  display_name: "Figthread"
  short_description: "Author semantic figures with deterministic layout and motion"
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
  default_prompt: "Use $figthread to author and validate semantic figure state, deterministic layout geometry, and explanatory semantic motion when motion adds meaning."
policy:
  allow_implicit_invocation: true
