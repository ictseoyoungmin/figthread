schema: hadara.skill/v0.1
name: figthread
version: 0.2.1
description: >-
  Design and validate semantic figures through promoted semantic state and
  deterministic promoted layout geometry.
entry:
  skill: SKILL.md
resources:
  templates: templates/
  references: references/
  assets: assets/
  schemas: schemas/
  runtime: runtime/
  scripts: scripts/
  examples: examples/
compatibility:
  agents: [Claude Code, Codex CLI]
  os: [windows, linux, macos]
interface:
  display_name: "Figthread"
  short_description: "Author semantic figures with deterministic layout gates"
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
  default_prompt: "Use $figthread to author and validate semantic figure state, then produce deterministic promoted layout geometry when layout is required."
policy:
  allow_implicit_invocation: true
