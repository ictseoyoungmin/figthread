schema: hadara.skill/v0.1
name: figthread
version: 0.2.0
description: >-
  Design and validate semantic figures through the FigureSpec gate and
  deterministic LayoutIntent/ResolvedLayout promotion gate.
entry:
  skill: SKILL.md
resources:
  templates: templates/
  references: references/
  assets: assets/
  schemas: schemas/
  runtime: runtime/
  scripts: scripts/
  examples: examples/
compatibility:
  agents: [Claude Code, Codex CLI]
  os: [windows, linux, macos]
interface:
  display_name: "Figthread"
  short_description: "Author semantic figures with deterministic layout gates"
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
  default_prompt: "Use $figthread to author, gate, and deterministically lay out a semantic figure before downstream rendering."
policy:
  allow_implicit_invocation: true
